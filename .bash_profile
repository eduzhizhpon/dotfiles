#
# ~/.bash_profile
#

# ==================================================
# Default PATHs
# ==================================================
[[ -f ~/.common_profile ]] && . ~/.common_profile

[[ -f ~/.bashrc ]] && . ~/.bashrc

